import { PageHeader } from '../../components/layout/PageHeader';

export function PackagesPage() {
  return (
    <PageHeader
      title="Package Automation"
      description="Package installation plans, supported package managers, and execution history."
    />
  );
}

