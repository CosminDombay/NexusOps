import { PageHeader } from '../../components/layout/PageHeader';

export function ProfilesPage() {
  return (
    <PageHeader
      title="Standardization Profiles"
      description="Reusable Linux user and group profiles for consistent server onboarding."
    />
  );
}

