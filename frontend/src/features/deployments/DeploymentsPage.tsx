import { PageHeader } from '../../components/layout/PageHeader';

export function DeploymentsPage() {
  return (
    <PageHeader
      title="Docker Deployments"
      description="Docker Compose projects deployed to managed Linux servers."
    />
  );
}

