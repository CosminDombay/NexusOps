import { PageHeader } from '../../components/layout/PageHeader';

export function ProvisioningPage() {
  return (
    <PageHeader
      title="VM Provisioning"
      description="Proxmox-backed virtual machine requests and lifecycle operations."
    />
  );
}

